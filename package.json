{
  "name": "humphrey",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Humphrey — noir murder mystery roguelike. Milestone 1: case generator and truth sheet.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "case": "tsx src/cli/case.ts",
    "batch": "tsx src/cli/batch.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.21",
    "vitest": "^2.1.8"
  }
}
